import Head from "next/head";

export default function Dashboard() {
    return (
        <div>
            <Head>
                <title>Meu painel de tarefas</title>
            </Head>

            <h1>Pagina Painel</h1>
        </div>
    )
}
